/** function for chacking if webpage is in some arbitrary black list
 * It is just a mock to show that we actually can check it
 */
async function isInBlackList(url) {
    return false;
}

exports.isInBlackList = isInBlackList;